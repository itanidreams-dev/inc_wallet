{
  "name": "inc_wallet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Simple secure Metani/iTani wallet scaffold using external signers only.",
  "scripts": {
    "config:validate": "node scripts/validate-network-config.cjs",
    "security:check": "node scripts/security-check.cjs",
    "test:e2e:plan": "node scripts/e2e-plan.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
